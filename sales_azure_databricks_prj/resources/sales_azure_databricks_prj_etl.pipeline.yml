# The main pipeline for sales_azure_databricks_prj

resources:
  pipelines:
    sales_azure_databricks_prj_etl:
      name: sales_azure_databricks_prj_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/sales_azure_databricks_prj_etl"

      libraries:
        - glob:
            include: ../src/sales_azure_databricks_prj_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
